{
  "name": "shielded-shell",
  "private": true,
  "version": "0.1.0",
  "description": "Zero-trust local safety harness for CLI coding agents",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @shieldedshell/core && npm run build -w shieldedshell",
    "test": "npm run test -w @shieldedshell/core",
    "lint": "npm run build",
    "shieldedshell": "node packages/cli/dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/connerkup/shielded-shell.git"
  }
}
